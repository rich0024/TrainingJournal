<%= form_for([@workout.lesson, @workout]) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <br>
    <%= f.label :deatils %>
    <%= f.text_area :details%>
    <br>
    <br>
    <%= f.submit 'Submit' %>
    <% end %>