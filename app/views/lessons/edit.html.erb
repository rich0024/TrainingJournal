<h1><%= @lesson.name %></h1>

<%= form_for(@lesson) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <br>
    <%= f.label :date %>
    <%= f.date_select :date %>
    <br>
    <br>
    <%= f.submit 'Submit' %>

    <% end %>
    <%= link_to 'Destroy', @lesson, method: :delete, data: { confirm: 'Are you sure?' } %>

    <%= link_to 'Back to Lessons', user_lessons_path(@lesson.user_id) %>